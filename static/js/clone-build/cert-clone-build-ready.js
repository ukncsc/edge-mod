// When the DOM is ready, set up the viewmodel...

define([
    "common/window-shim",
    "common/load-draft"
], function (window, loadDraft) {
    "use strict";



    loadDraft(window.draft_id)
});
